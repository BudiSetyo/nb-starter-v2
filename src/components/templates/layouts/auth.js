const Auth = ({ children }) => {
  return (
    <main className="container mx-auto my-auto min-h-screen bg-gray-100">
      <main className="py-20 px-10">{children}</main>
    </main>
  )
}

export default Auth
